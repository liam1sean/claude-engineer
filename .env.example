# ── API service (root) ──────────────────────────────────────────────────────

# Anthropic API key — get from https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-...

# Optional: x-api-key secret for local dev (Cloud Run IAM protects production).
# Generate: openssl rand -hex 32
SERVICE_API_KEY=change-me-to-a-strong-random-secret

# Optional overrides
# PORT=3000
# CLAUDE_MODEL=claude-sonnet-4-20250514
# CLAUDE_MAX_TOKENS=800


# ── Web service (services/web) ──────────────────────────────────────────────

# URL of the API service this web service should proxy to.
# Local dev:  http://localhost:3000
# Production: set automatically by deploy.yml via --set-env-vars
API_CLAUDE_URL=http://localhost:3000

# Optional: forwarded as x-api-key when API_CLAUDE_URL is localhost.
# Must match SERVICE_API_KEY in the API service .env (local dev only).
API_SERVICE_KEY=change-me-to-a-strong-random-secret
